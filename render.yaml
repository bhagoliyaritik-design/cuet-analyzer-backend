services:
  - type: web
    name: cuet-analyzer
    env: python
    plan: free
    region: oregon
    buildCommand: pip install -r requirements.txt
    startCommand: gunicorn scraper:app --bind 0.0.0.0:$PORT
